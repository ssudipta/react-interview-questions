import React from "react";

const Loader: React.FC = () => {
  return (
    <div className="suggestion-box">
    <ul className="item-list item">Loading...</ul>
    </div>
  );
};

export default Loader;
